import { Router } from "express";
// import { createMockCompany, createMockJob } from "../controllers/job.controller";

const testRouter = Router();

// testRouter.post('/test/mock-job', createMockJob);
// testRouter.post('/test/mock-company', createMockCompany);

export default testRouter;